[pytest]
testpaths = tests
pythonpath = backend
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    canli_ai: Gerçek AI sağlayıcısına istek atan testler (varsayılan olarak atlanır)
    yavas: Uzun süren testler
